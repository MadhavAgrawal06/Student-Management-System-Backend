# Project name
spring.application.name=student-backend

# Render requires dynamic port
server.port=${PORT:8080}

# --- Database Configuration ---
# This uses the variables you will input into the Render Dashboard
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?ssl-mode=REQUIRED
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}

# Driver and Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

